test:
  image: node:latest

  cache:
    paths:
      - node_modules/
  
  before_script:
    - npm install

  script:
    - npm run test:coverage
    - npm run build
  
  coverage: /All\sfiles.*?\s+(\d+.\d+)/
